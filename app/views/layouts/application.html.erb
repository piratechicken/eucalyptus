<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Eucalyptus" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
  <% unless controller_name == "landing" %>
   <nav class="top-bar" data-topbar role="navigation">
      <ul class="title-area">
        <li class="name">
          <h1><%= link_to 'Eucalyptus', root_path %></h1>
        </li>
        <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
        <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
      </ul>

      <section class="top-bar-section">
        <!-- Right Nav Section -->
        <ul class="right">
        <% if user_signed_in? %>
          <% if current_user.profile.present? %>
            <li><%= link_to 'My profile', current_user.profile, class: 'btn btn-secondary' %></li>
          <% else %>
            <li><%= link_to 'Create profile', new_profile_path, class: 'btn btn-secondary'  %></li>
          <% end %>
          <li><%= link_to 'Sign out', destroy_user_session_path, class: "button", method: :delete %></li>
        <% else %>
          <li><%= link_to 'Log in', new_user_session_path, class: "button" %></li>
        <% end %>
        </ul>

        <!-- Left Nav Section -->
        <ul class="left">
          <li><a href="#">Left Nav Button1</a></li>
          <li><a href="#">Left Nav Button2</a></li>
        </ul>
      </section>
    </nav>
  <% end %>

    <% if notice.present? || alert.present? %>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%#= alert %></p>
    <% end %>

    <%= yield %>
    <%= javascript_include_tag "application" %>
  </body>
</html>