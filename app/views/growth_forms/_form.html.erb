<%= form_with(model: growth_form, local: true) do |form| %>
  <% if growth_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(growth_form.errors.count, "error") %> prohibited this growth_form from being saved:</h2>

      <ul>
      <% growth_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :growth_form_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :growth_form_description %>
  </div>

  <div class="field">
    <%= form.label :image_data %>
    <%= form.text_area :image_data, id: :growth_form_image_data %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
